import React from 'react';
import { Text } from 'react-native';

const DiscoveryScreen = () => {
 return (
  <Text style = {{ textAlign: 'center', marginTop: '60%' }} >DiscoveryScreen</Text>   
 )   
};

export default DiscoveryScreen;