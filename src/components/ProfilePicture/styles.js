import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
 image: {
  borderRadius: 40,
  borderWidth: 1,
  borderColor : '#ffffff',
  marginTop: 3,
  marginLeft: 3
 },
 container: {
  margin: 10 
 }   
});

export default styles;